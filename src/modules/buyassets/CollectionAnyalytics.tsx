import { Box } from '@mui/material';
import { FC } from 'react';
// eslint-disable-next-line @typescript-eslint/no-empty-interface
interface CollactionAnalyticsProps {}
const CollactionAnalytics: FC<CollactionAnalyticsProps> = () => {
  return <Box>M</Box>;
};

export default CollactionAnalytics;
