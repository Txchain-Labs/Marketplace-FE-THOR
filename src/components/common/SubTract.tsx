import { Icon } from '@mui/material';
import Image from 'next/image';

export default function SubTract() {
  return (
    <Icon>
      <Image alt="sub-tract" src="/images/Stract.svg" height={26} width={18} />
    </Icon>
  );
}
