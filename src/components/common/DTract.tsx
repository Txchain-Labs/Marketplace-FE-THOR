import { Icon } from '@mui/material';
import Image from 'next/image';

export default function DTract() {
  return (
    <Icon>
      <Image alt="D-tract" src="/images/Dtract.svg" height={24} width={16} />
    </Icon>
  );
}
