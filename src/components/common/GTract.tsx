import { Icon } from '@mui/material';
import Image from 'next/image';

export default function GTract() {
  return (
    <Icon>
      <Image alt="D-tract" src="/images/Gtract.svg" height={24} width={16} />
    </Icon>
  );
}
