import { palette } from '../theme/palette';

export const badgeStyle = {
  background: palette.primary.storm,
  p: 0.5,
  border: `1px solid ${palette.primary.ash}`,
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
};
