import React from 'react';
import { Profile } from '../../src/modules/profile';

const profile = () => {
  return (
    <div>
      <Profile defaultTab={1} />
    </div>
  );
};

export default profile;
